{{- if .Values.admin.enabled }}
{{- $adminContainerPort := .Values.admin.service.port }}
{{- if and .Values.admin.listenAddr (regexMatch "^.*:[0-9]+$" .Values.admin.listenAddr) }}
{{- $adminContainerPort = (regexReplaceAll "^.*:([0-9]+)$" .Values.admin.listenAddr "$1" | int) }}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "llm-proxy.admin.fullname" . }}
  labels:
    {{- include "llm-proxy.admin.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "llm-proxy.admin.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "llm-proxy.admin.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: admin
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - "admin"
            - "--listen"
            - {{ .Values.admin.listenAddr | quote }}
            - "--api-base-url"
            - {{ default (printf "http://%s:%v" (include "llm-proxy.fullname" .) .Values.service.port) .Values.admin.apiBaseUrl | quote }}
          ports:
            - name: http
              containerPort: {{ $adminContainerPort }}
              protocol: TCP
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- $managementTokenSecret := include "llm-proxy.managementTokenSecretName" . }}
          {{- $hasEnv := and (hasKey .Values "env") (gt (len .Values.env) 0) }}
          {{- if or $hasEnv $managementTokenSecret }}
          env:
          {{- if $hasEnv }}
          {{- range $key := keys .Values.env | sortAlpha }}
            - name: {{ $key }}
              value: {{ index $.Values.env $key | quote }}
          {{- end }}
          {{- end }}
          {{- if $managementTokenSecret }}
            - name: MANAGEMENT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ $managementTokenSecret }}
                  key: {{ include "llm-proxy.managementTokenSecretKey" . }}
          {{- end }}
          {{- end }}
          {{- if .Values.volumeMounts }}
          volumeMounts:
            {{- toYaml .Values.volumeMounts | nindent 12 }}
          {{- end }}
      {{- with .Values.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
